<%- include("../partials/layout_top", { title }) %>
<div class="d-flex justify-content-between align-items-end flex-wrap gap-2 mb-3">
  <div>
    <h1 class="h4 mb-1">Статистика</h1>
    <div class="text-white-50">Сводные показатели и рейтинги фестивалей (Chart.js).</div>
  </div>
</div>

<div class="row g-3">
  <div class="col-12 col-lg-5">
    <div class="card card-glass shadow-sm">
      <div class="card-body p-4">
        <h2 class="h6 mb-3">Сводные показатели</h2>
        <div class="row g-3">
          <div class="col-6"><div class="stat-tile"><div class="stat-v"><%= totals.users %></div><div class="stat-l">Пользователи</div></div></div>
          <div class="col-6"><div class="stat-tile"><div class="stat-v"><%= totals.festivals %></div><div class="stat-l">Фестивали</div></div></div>
          <div class="col-6"><div class="stat-tile"><div class="stat-v"><%= totals.artists %></div><div class="stat-l">Артисты</div></div></div>
          <div class="col-6"><div class="stat-tile"><div class="stat-v"><%= totals.performances %></div><div class="stat-l">Выступления</div></div></div>
          <div class="col-12"><div class="stat-tile"><div class="stat-v"><%= totals.reviews %></div><div class="stat-l">Отзывы/оценки</div></div></div>
        </div>
        <div class="text-white-50 small mt-3">
          Средний разрыв между выступлениями на одной сцене: <span class="text-white fw-semibold"><%= avgGapMin %> мин</span>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12 col-lg-7">
    <div class="card card-glass shadow-sm">
      <div class="card-body p-4">
        <div class="d-flex justify-content-between align-items-center">
          <h2 class="h6 mb-0">Топ фестивалей по средней оценке</h2>
          <div class="small text-white-50">учитывается количество отзывов</div>
        </div>
        <div class="mt-3">
          <canvas id="chartRatings" height="150"></canvas>
        </div>
        <div class="table-responsive mt-3">
          <table class="table table-dark table-hover align-middle">
            <thead>
              <tr>
                <th>Фестиваль</th>
                <th class="text-end">Средняя оценка</th>
                <th class="text-end">Отзывов</th>
              </tr>
            </thead>
            <tbody>
              <% chart.forEach(r => { %>
                <tr>
                  <td><%= r.name %></td>
                  <td class="text-end"><%= r.avg_rating %></td>
                  <td class="text-end"><%= r.reviews %></td>
                </tr>
              <% }) %>
              <% if (!chart.length) { %>
                <tr><td colspan="3" class="text-center text-white-50 py-4">Нет данных</td></tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
<script>
  const labels = <%- JSON.stringify(chart.map(x => x.name)) %>;
  const avg = <%- JSON.stringify(chart.map(x => Number(x.avg_rating))) %>;
  const reviews = <%- JSON.stringify(chart.map(x => Number(x.reviews))) %>;

  const ctx = document.getElementById('chartRatings');
  new Chart(ctx, {
    type: 'bar',
    data: {
      labels,
      datasets: [
        { label: 'Средняя оценка', data: avg },
        { label: 'Количество отзывов', data: reviews }
      ]
    },
    options: {
      responsive: true,
      scales: {
        y: { beginAtZero: true }
      }
    }
  });
</script>

<%- include("../partials/layout_bottom") %>
