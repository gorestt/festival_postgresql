<!doctype html>
<html lang="ru">
  <head><%- include("../partials/head", { title }) %></head>
  <body class="auth-body">
    <main class="auth-wrap">
      <div class="auth-card shadow-lg">
        <div class="auth-hero">
          <div class="brand-mark lg">FDB</div>
          <h1 class="h4 mb-1">FestivalDB</h1>
          <p class="text-white-50 mb-0">Информационно-справочная система музыкальных фестивалей</p>
        </div>

        <div class="p-4 p-md-5">
          <% if (flash.error && flash.error.length) { %>
            <div class="alert alert-danger"><%= flash.error[0] %></div>
          <% } %>
          <% if (flash.success && flash.success.length) { %>
            <div class="alert alert-success"><%= flash.success[0] %></div>
          <% } %>

          <h2 class="h5 mb-3">Вход</h2>
          <form method="post" action="/login" class="vstack gap-3">
            <div>
              <label class="form-label">Email</label>
              <input name="email" type="email" class="form-control" required placeholder="you@example.com">
            </div>
            <div>
              <label class="form-label">Пароль</label>
              <input name="password" type="password" class="form-control" required placeholder="Минимум 8 символов">
            </div>
            <button class="btn btn-primary w-100" type="submit"><i class="bi bi-box-arrow-in-right me-1"></i>Войти</button>
          </form>

          <div class="text-center mt-3">
            <a href="/register" class="link-light-subtle">Нет аккаунта? Зарегистрироваться</a>
          </div>

          <hr class="my-4">
          <div class="small text-white-50">
            Демо-аккаунты:
            <span class="badge bg-soft">admin@demo.local / Admin123!</span>
            <span class="badge bg-soft">org@demo.local / Org123!</span>
            <span class="badge bg-soft">viewer@demo.local / Viewer123!</span>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
