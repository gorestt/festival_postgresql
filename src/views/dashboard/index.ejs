<%- include("../partials/layout_top", { title }) %>
<%- include("../partials/flash") %>
<div class="row g-4">
  <div class="col-12 col-lg-7">
    <div class="card card-glass shadow-sm">
      <div class="card-body p-4">
        <div class="d-flex align-items-center justify-content-between gap-3 flex-wrap">
          <div>
            <h1 class="h4 mb-1">Панель управления</h1>
            <div class="text-white-50">Краткий обзор базы фестивалей, артистов и расписаний.</div>
          </div>
          <div class="d-flex gap-2">
            <a class="btn btn-primary" href="/festivals"><i class="bi bi-calendar2-week me-1"></i>Открыть каталог</a>
            <% if (isAnyRole(["ORGANIZER","ADMIN"])) { %>
              <a class="btn btn-outline-light" href="/festivals/new"><i class="bi bi-plus-circle me-1"></i>Добавить фестиваль</a>
            <% } %>
          </div>
        </div>

        <div class="row g-3 mt-3">
          <div class="col-12 col-md-4">
            <div class="kpi">
              <div class="kpi-icon"><i class="bi bi-people"></i></div>
              <div>
                <div class="kpi-value"><%= kpis.users %></div>
                <div class="kpi-label">пользователей</div>
              </div>
            </div>
          </div>
          <div class="col-12 col-md-4">
            <div class="kpi">
              <div class="kpi-icon"><i class="bi bi-calendar-event"></i></div>
              <div>
                <div class="kpi-value"><%= kpis.festivals %></div>
                <div class="kpi-label">фестивалей</div>
              </div>
            </div>
          </div>
          <div class="col-12 col-md-4">
            <div class="kpi">
              <div class="kpi-icon"><i class="bi bi-music-note-list"></i></div>
              <div>
                <div class="kpi-value"><%= kpis.performances %></div>
                <div class="kpi-label">выступлений</div>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-4">
          <div class="small text-white-50">Советы по работе:</div>
          <ul class="text-white-50 mb-0">
            <li>Организаторы добавляют фестивали, сцены и выступления.</li>
            <li>Зрители оценивают фестивали и оставляют отзывы.</li>
            <li>В разделе «Статистика» доступна визуализация рейтингов и метрик расписания.</li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12 col-lg-5">
    <div class="card card-glass shadow-sm">
      <div class="card-body p-4">
        <div class="d-flex justify-content-between align-items-center">
          <h2 class="h6 mb-0">Недавно добавленные фестивали</h2>
          <a class="small link-light-subtle" href="/festivals">Все фестивали</a>
        </div>
        <div class="mt-3 vstack gap-3">
          <% if (!recent.length) { %>
            <div class="text-white-50">Пока нет данных.</div>
          <% } %>
          <% recent.forEach(f => { %>
            <a class="card mini-card text-decoration-none" href="/festivals/<%= f.id %>">
              <div class="card-body">
                <div class="d-flex justify-content-between gap-2">
                  <div>
                    <div class="fw-semibold text-white"><%= f.name %></div>
                    <div class="small text-white-50"><i class="bi bi-geo-alt me-1"></i><%= f.city %>, <%= f.country %></div>
                  </div>
                  <div class="text-end">
                    <div class="badge bg-soft"><%= f.genre %></div>
                    <div class="small text-white-50 mt-1">
                      <% if (f.avg_rating) { %>
                        <i class="bi bi-star-fill me-1"></i><%= f.avg_rating %> (<%= f.reviews %>)
                      <% } else { %>
                        <span class="text-white-50">нет оценок</span>
                      <% } %>
                    </div>
                  </div>
                </div>
                <div class="small text-white-50 mt-2">
                  <i class="bi bi-calendar3 me-1"></i>
                  <%= new Date(f.start_date).toLocaleDateString("ru-RU") %> — <%= new Date(f.end_date).toLocaleDateString("ru-RU") %>
                </div>
              </div>
            </a>
          <% }) %>
        </div>
      </div>
    </div>
  </div>
</div>
<%- include("../partials/layout_bottom") %>
